<UserControl x:Class="JsonSurfer.App.Views.Controls.JsonTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:JsonSurfer.Core.Models;assembly=JsonSurfer.Core"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- TreeView Toolbar -->
        <Border Grid.Row="0" Background="#F0F0F0" Padding="8,4" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="Expand All" Command="{Binding ExpandAllNodesCommand}" Margin="0,0,8,0" Padding="8,4" FontSize="11" />
                <Button Content="Collapse All" Command="{Binding CollapseAllNodesCommand}" Margin="0,0,8,0" Padding="8,4" FontSize="11" />
                <TextBlock Text="TreeView Controls" Foreground="Gray" VerticalAlignment="Center" FontSize="10" FontStyle="Italic" />
            </StackPanel>
        </Border>
        
        <!-- TreeView with Expansion Binding -->
        <TreeView x:Name="JsonTree" Grid.Row="1" ItemsSource="{Binding RootNode.Children, FallbackValue={x:Null}}" SelectedItemChanged="JsonTree_SelectedItemChanged">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                    <Setter Property="FontSize" Value="{Binding DataContext.TreeViewFontSize, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue=11}" />
                    <Setter Property="ItemsSource" Value="{Binding Children}" />
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
    </Grid>
</UserControl>